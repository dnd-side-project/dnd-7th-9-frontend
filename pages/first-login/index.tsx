import type { NextPage } from 'next';
import FirstLoginScreen from '@app.feature/first-login/screen/FirstLoginScreen';

const FirstLogin: NextPage = () => {
	return <FirstLoginScreen />;
};

export default FirstLogin;
